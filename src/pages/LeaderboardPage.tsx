export function LeaderboardPage() {
  return (
    <div style={{
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      height: '100%',
      padding: 32,
    }}>
      <p style={{ fontSize: 15, color: 'var(--text-muted)' }}>
        User-wide leaderboard coming soon.
      </p>
    </div>
  )
}
